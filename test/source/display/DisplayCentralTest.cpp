#include "display/DisplayCentralTest.h"
#include <cassert>

void DisplayCentralTest::run()
{
	testSetThisFrame();
	testGetDiff();
}

void DisplayCentralTest::testSetThisFrame()
{
	TestableDisplayCentral dc;
	// Make frames
	//frameFirst = 
	//frameSecond = 
	//dc.setThisFrame( frameFirst );
	//assert( dc.frameLast == Frame::FRAME_BLANK );
	//assert( dc.frameThis == frameFirst );
	//dc.setThisFrame( frameSecond );
	//assert( dc.frameLast == frameFirst );
	//assert( dc.frameThis == frameSecond );
}

void DisplayCentralTest::testGetDiff()
{
	TestableDisplayCentral dc;
	// Make frames
	//frameFirst = 
	//frameSecond = 
	//dc.setThisFrame( frameFirst );
	//dc.setThisFrame( frameSecond );
	//Make Diff
	//assert( dc.getDiff() == ??? );
}
